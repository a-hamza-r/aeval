#include "declarations.h"

//	control flow
//	vector if/gotos

int s1279(int count) {
if (count <= 0 || count > 10) return 1;
	for (int i = 0; i < count*8; i++) {
		if (a[i] < 0) {
			if (b[i] > a[i]) {
				c[i] += d[i] * e[i];
			}
		}
	}
  return 0;
}
