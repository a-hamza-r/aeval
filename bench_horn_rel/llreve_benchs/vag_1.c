#include "declarations.h"

//    control loops
//    vector assignment, gather
//    gather is required

int vag(int count) {
if (count <= 0 || count > 10) return 1;
  for (int i = 0; i < count*8; i++) {
    a[i] = b[ip[i]];
  }
  return 0;
}


