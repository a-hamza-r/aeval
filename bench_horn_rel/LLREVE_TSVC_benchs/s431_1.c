#include "declarations.h"

//    parameters
//    parameter statement

int s431(int count) {
if (count <= 0 || count > 10) return 1;
  int k=0;
  for (int i = 0; i < count*8; i++) {
    a[i] = a[i+k] + b[i];
  }
  return 0;
}

