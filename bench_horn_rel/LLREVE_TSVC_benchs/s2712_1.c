#include "declarations.h"

//	control flow
//	if to elemental min

int s2712(int count) {
if (count <= 0 || count > 10) return 1;
	for (int i = 0; i < count*8; i++) {
		if (a[i] > b[i]) {
			a[i] += b[i] * c[i];
		}
	}
  return 0;
}

